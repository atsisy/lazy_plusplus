#ifndef _LZ_ARRAY_HPP
#define _LZ_ARRAY_HPP

//#include <array>
//#include <functional>
//
//namespace lzy {
//
//	template <typename T, std::size_t size> class array : public std::array<T, int>, public LZCORE<T> {
//	public:
//
//		void for_each(std::function<void(T)> function) {
//			unsigned int size = this->size();
//			for (unsigned int i = 0; i < size; ++i) {
//				function(this->at(i));
//			}
//		}
//	};
//
//}

#endif